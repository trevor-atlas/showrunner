# Agent module note

This file lives next to the handoff blueprint module. Context entries that do
not resolve against the run's cwd fall back to the agent module's directory
 — "agent-notes.md" is the fallback-branch proof.
