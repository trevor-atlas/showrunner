# dependencies
node_modules/

# builds
dist/
build/
coverage/
.turbo/

# env & secrets
.env
.env.*

# local run data
/runs/
*.db
*.db-wal
*.db-shm

# run-time handoff materialization under the run cwd (FINDING-3: a run with
# the default cwd must never dirty the checkout — the README quickstart runs
# in a scratch --cwd, and this belt-and-suspenders covers runs that don't)
context_handoff/

# misc
.DS_Store
*.log
